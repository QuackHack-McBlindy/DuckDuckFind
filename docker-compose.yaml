version: '3.8'

services:
  duckduckfind:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5556:5556"
    volumes:
      - ./documents:/app/documents
    environment:
      - FLASK_ENV=production
    command: sh -c "python app.py"

